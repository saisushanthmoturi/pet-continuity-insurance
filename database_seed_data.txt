-- ================================================================================
-- PET CONTINUITY INSURANCE - COMPLETE 34-TABLE MASTER DDL & SEED DATA SCRIPT
-- ================================================================================
-- Database System: MySQL 8.x / MariaDB
-- Architecture: 8 Independent Microservice Databases, 34 Tables
-- Authentication: Stateless JWT via HttpOnly Cookie (jwt_token) / Bearer header fallback
-- Audit Rule: All timestamps (`created_at`, `updated_at`, `submitted_at`, etc.) are 
--             autogenerated via `DEFAULT CURRENT_TIMESTAMP` and 
--             `ON UPDATE CURRENT_TIMESTAMP` where supported.

-- ================================================================================
-- 1. AUTH SERVICE (Database: auth_db)
-- ================================================================================
CREATE DATABASE IF NOT EXISTS auth_db;
USE auth_db;

SET FOREIGN_KEY_CHECKS = 0;
DROP TABLE IF EXISTS refresh_tokens, users;
SET FOREIGN_KEY_CHECKS = 1;

-- 1. users
CREATE TABLE IF NOT EXISTS users (
    user_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(100),
    email VARCHAR(150) NOT NULL UNIQUE,
    password_hash VARCHAR(255) NOT NULL,
    role VARCHAR(50) NOT NULL,
    status VARCHAR(50) DEFAULT 'ACTIVE',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    last_login_at TIMESTAMP NULL
);

-- Seed Data for auth_db
-- Password for all seed users is 'Password123!' (BCrypt: $2a$10$eAccYoNO32OmAr8cb9WCJewekWVJnPW4/H1E9K1E4w29R5G0b6EKe)
INSERT INTO users (user_id, username, email, password_hash, role, status, last_login_at) VALUES
(1, 'john_doe', 'john.doe@example.com', '$2a$10$eAccYoNO32OmAr8cb9WCJewekWVJnPW4/H1E9K1E4w29R5G0b6EKe', 'CUSTOMER', 'ACTIVE', NOW()),
(2, 'sarah_underwriter', 'sarah.uw@example.com', '$2a$10$eAccYoNO32OmAr8cb9WCJewekWVJnPW4/H1E9K1E4w29R5G0b6EKe', 'UNDERWRITER', 'ACTIVE', NOW()),
(3, 'mike_claims', 'mike.claims@example.com', '$2a$10$eAccYoNO32OmAr8cb9WCJewekWVJnPW4/H1E9K1E4w29R5G0b6EKe', 'CLAIMS_OFFICER', 'ACTIVE', NOW()),
(4, 'emma_caretaker', 'emma.caretaker@example.com', '$2a$10$eAccYoNO32OmAr8cb9WCJewekWVJnPW4/H1E9K1E4w29R5G0b6EKe', 'CARETAKER', 'ACTIVE', NOW()),
(5, 'admin_master', 'admin@petcontinuity.com', '$2a$10$eAccYoNO32OmAr8cb9WCJewekWVJnPW4/H1E9K1E4w29R5G0b6EKe', 'ADMIN', 'ACTIVE', NOW())
ON DUPLICATE KEY UPDATE updated_at = NOW();

--
-- ================================================================================
-- 2. CUSTOMER SERVICE (Database: customer_db)
-- ================================================================================
CREATE DATABASE IF NOT EXISTS customer_db;
USE customer_db;

SET FOREIGN_KEY_CHECKS = 0;
DROP TABLE IF EXISTS addresses, customers;
SET FOREIGN_KEY_CHECKS = 1;

-- 3. customers
CREATE TABLE IF NOT EXISTS customers (
    customer_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    id BIGINT,
    user_id BIGINT NOT NULL UNIQUE,
    first_name VARCHAR(100) NOT NULL,
    last_name VARCHAR(100) NOT NULL,
    contact_email VARCHAR(150) NOT NULL,
    email VARCHAR(150),
    phone VARCHAR(50),
    date_of_birth DATE,
    status VARCHAR(50) DEFAULT 'ACTIVE',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 4. addresses
CREATE TABLE IF NOT EXISTS addresses (
    address_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    id BIGINT,
    address_type VARCHAR(50) DEFAULT 'HOME',
    customer_id BIGINT NOT NULL,
    line1 VARCHAR(255) NOT NULL,
    line2 VARCHAR(255),
    city VARCHAR(100) NOT NULL,
    state VARCHAR(100) NOT NULL,
    postal_code VARCHAR(20) NOT NULL,
    country VARCHAR(100) NOT NULL DEFAULT 'US',
    CONSTRAINT fk_address_customer FOREIGN KEY (customer_id) REFERENCES customers(customer_id) ON DELETE CASCADE
);

-- Seed Data for customer_db
INSERT INTO customers (customer_id, user_id, first_name, last_name, contact_email, email, phone, date_of_birth, status) VALUES
(1, 1, 'John', 'Doe', 'john.doe@example.com', 'john.doe@example.com', '+1-555-0199', '1985-06-15', 'ACTIVE'),
(2, 4, 'Emma', 'Watson', 'emma.caretaker@example.com', 'emma.caretaker@example.com', '+1-555-0244', '1990-09-20', 'ACTIVE')
ON DUPLICATE KEY UPDATE updated_at = NOW();

INSERT INTO addresses (address_id, address_type, customer_id, line1, line2, city, state, postal_code, country) VALUES
(1, 'HOME', 1, '742 Evergreen Terrace', 'Apt 4B', 'Springfield', 'OR', '97477', 'US'),
(2, 'BILLING', 1, '100 Financial Way', 'Suite 200', 'Springfield', 'OR', '97477', 'US')
ON DUPLICATE KEY UPDATE city = VALUES(city);

-- ================================================================================
-- 3. PET SERVICE (Database: pet_db)
-- ================================================================================
CREATE DATABASE IF NOT EXISTS pet_db;
USE pet_db;

SET FOREIGN_KEY_CHECKS = 0;
DROP TABLE IF EXISTS pet_medical_records, pets;
SET FOREIGN_KEY_CHECKS = 1;

-- 5. pets
CREATE TABLE IF NOT EXISTS pets (
    pet_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    id BIGINT,
    customer_id BIGINT NOT NULL,
    date_of_birth_estimated BOOLEAN DEFAULT FALSE,
    name VARCHAR(100) NOT NULL,
    currency VARCHAR(10) DEFAULT 'USD',
    species_code VARCHAR(50) NOT NULL,
    breed_code VARCHAR(100) NOT NULL,
    species VARCHAR(50),
    breed VARCHAR(100),
    gender VARCHAR(20) NOT NULL,
    status VARCHAR(50) DEFAULT 'ACTIVE',
    date_of_birth DATE,
    age INT,
    weight_value DOUBLE,
    weight DOUBLE,
    microchip_id VARCHAR(100) UNIQUE,
    weight_unit VARCHAR(10) DEFAULT 'KG',
    annual_care_cost DOUBLE DEFAULT 1200.0,
    expected_remaining_years INT DEFAULT 10,
    neutered_status BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 6. pet_medical_records
CREATE TABLE IF NOT EXISTS pet_medical_records (
    medical_record_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    id BIGINT,
    pet_id BIGINT NOT NULL,
    record_type VARCHAR(50) NOT NULL,
    file_url VARCHAR(500) NOT NULL,
    file_hash VARCHAR(128),
    file_name VARCHAR(255) NOT NULL,
    file_size_bytes BIGINT NOT NULL,
    mime_type VARCHAR(100) NOT NULL,
    description TEXT,
    recorded_date DATE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    CONSTRAINT fk_med_pet FOREIGN KEY (pet_id) REFERENCES pets(pet_id) ON DELETE CASCADE
);

-- Seed Data for pet_db
INSERT INTO pets (pet_id, customer_id, name, species_code, breed_code, species, breed, gender, status, date_of_birth, age, weight_value, weight, microchip_id, weight_unit, annual_care_cost, expected_remaining_years, neutered_status) VALUES
(1, 1, 'Buddy', 'DOG', 'GOLDEN_RETRIEVER', 'DOG', 'Golden Retriever', 'MALE', 'ACTIVE', '2021-04-10', 3, 31.5, 31.5, 'CHIP-982000412345678', 'KG', 1800.0, 11, TRUE),
(2, 1, 'Luna', 'CAT', 'SIAMESE', 'CAT', 'Siamese', 'FEMALE', 'ACTIVE', '2022-08-18', 2, 4.2, 4.2, 'CHIP-982000987654321', 'KG', 900.0, 14, TRUE)
ON DUPLICATE KEY UPDATE updated_at = NOW();

INSERT INTO pet_medical_records (medical_record_id, pet_id, record_type, file_url, file_hash, file_name, file_size_bytes, mime_type, description, recorded_date) VALUES
(1, 1, 'VACCINATION', 'https://petcontinuity-records.s3.amazonaws.com/pets/1/rabies_cert_2024.pdf', 'e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855', 'rabies_cert_2024.pdf', 245800, 'application/pdf', 'Annual rabies vaccination certificate', '2024-04-15'),
(2, 1, 'CHECKUP', 'https://petcontinuity-records.s3.amazonaws.com/pets/1/annual_checkup_2024.pdf', 'f4c1e44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b866', 'annual_checkup_2024.pdf', 512000, 'application/pdf', 'Comprehensive senior wellness exam', '2024-05-10')
ON DUPLICATE KEY UPDATE updated_at = NOW();

-- ================================================================================
-- 4. UNDERWRITING & RISK SERVICE (Database: underwriting_db)
-- ================================================================================
CREATE DATABASE IF NOT EXISTS underwriting_db;
USE underwriting_db;

SET FOREIGN_KEY_CHECKS = 0;
DROP TABLE IF EXISTS premium_calculations, rating_rules, coverage_assessments, risk_assessments, quotes;
SET FOREIGN_KEY_CHECKS = 1;

-- 7. quotes
CREATE TABLE IF NOT EXISTS quotes (
    quote_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    id BIGINT,
    pet_id BIGINT NOT NULL,
    customer_id BIGINT NOT NULL,
    quote_number VARCHAR(100) UNIQUE,
    status VARCHAR(50) DEFAULT 'ACTIVE',
    quote_type VARCHAR(50) DEFAULT 'CONTINUITY',
    requested_coverage_amount DOUBLE NOT NULL,
    coverage_amount DOUBLE,
    estimated_annual_cost DOUBLE NOT NULL,
    annual_cost DOUBLE,
    deductible_amount DOUBLE NOT NULL,
    deductible DOUBLE,
    total_cost DOUBLE NOT NULL,
    currency VARCHAR(10) DEFAULT 'USD',
    expires_at TIMESTAMP NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 8. risk_assessments
CREATE TABLE IF NOT EXISTS risk_assessments (
    assessment_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    id BIGINT,
    quote_id BIGINT NOT NULL,
    risk_tier VARCHAR(50) NOT NULL,
    base_premium DOUBLE NOT NULL,
    breed_risk_factor DOUBLE NOT NULL,
    age_risk_factor DOUBLE NOT NULL,
    final_premium DOUBLE NOT NULL,
    notes TEXT,
    approved_coverage_amount DOUBLE NOT NULL,
    risk_score DOUBLE NOT NULL,
    health_risk_factor DOUBLE NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    CONSTRAINT fk_risk_quote FOREIGN KEY (quote_id) REFERENCES quotes(quote_id) ON DELETE CASCADE
);

-- 9. coverage_assessments
CREATE TABLE IF NOT EXISTS coverage_assessments (
    coverage_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    id BIGINT,
    assessment_id BIGINT NOT NULL,
    coverage_type VARCHAR(100) NOT NULL,
    requested_limit DOUBLE NOT NULL,
    approved_limit DOUBLE NOT NULL,
    deductible DOUBLE NOT NULL,
    status VARCHAR(50) NOT NULL DEFAULT 'APPROVED',
    rejection_reason VARCHAR(255),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    CONSTRAINT fk_cov_assessment FOREIGN KEY (assessment_id) REFERENCES risk_assessments(assessment_id) ON DELETE CASCADE
);

-- 10. rating_rules
CREATE TABLE IF NOT EXISTS rating_rules (
    rule_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    rule_name VARCHAR(100) NOT NULL,
    species_code VARCHAR(50) NOT NULL,
    breed_code VARCHAR(100),
    min_age_years INT DEFAULT 0,
    max_age_years INT DEFAULT 30,
    factor DOUBLE NOT NULL,
    active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 11. premium_calculations
CREATE TABLE IF NOT EXISTS premium_calculations (
    calculation_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    quote_id BIGINT NOT NULL,
    base_rate DOUBLE NOT NULL,
    breed_multiplier DOUBLE NOT NULL,
    age_multiplier DOUBLE NOT NULL,
    health_multiplier DOUBLE NOT NULL,
    calculated_annual_premium DOUBLE NOT NULL,
    calculated_monthly_premium DOUBLE NOT NULL,
    calculated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    currency VARCHAR(10) DEFAULT 'USD',
    rule_set_version VARCHAR(50) DEFAULT 'v1.0'
);

-- Seed Data for underwriting_db
INSERT INTO quotes (quote_id, pet_id, customer_id, quote_number, status, quote_type, requested_coverage_amount, estimated_annual_cost, deductible_amount, total_cost, currency, expires_at) VALUES
(1, 1, 1, 'QUO-2024-DOG-001', 'ACTIVE', 'CONTINUITY', 25000.0, 1800.0, 500.0, 850.0, 'USD', DATE_ADD(NOW(), INTERVAL 30 DAY)),
(2, 2, 1, 'QUO-2024-CAT-002', 'ACTIVE', 'CONTINUITY', 15000.0, 900.0, 250.0, 520.0, 'USD', DATE_ADD(NOW(), INTERVAL 30 DAY))
ON DUPLICATE KEY UPDATE updated_at = NOW();

INSERT INTO risk_assessments (assessment_id, quote_id, risk_tier, base_premium, breed_risk_factor, age_risk_factor, final_premium, notes, approved_coverage_amount, risk_score, health_risk_factor) VALUES
(1, 1, 'STANDARD', 600.0, 1.15, 1.10, 850.0, 'Good health, vaccinated, no pre-existing severe issues', 25000.0, 22.5, 1.05),
(2, 2, 'PREFERRED', 400.0, 1.05, 1.05, 520.0, 'Indoor cat, up-to-date vaccinations', 15000.0, 12.0, 1.00)
ON DUPLICATE KEY UPDATE updated_at = NOW();

INSERT INTO coverage_assessments (coverage_id, assessment_id, coverage_type, requested_limit, approved_limit, deductible, status) VALUES
(1, 1, 'CONTINUITY_CARE', 25000.0, 25000.0, 500.0, 'APPROVED'),
(2, 1, 'VET_EMERGENCY', 5000.0, 5000.0, 250.0, 'APPROVED'),
(3, 2, 'CONTINUITY_CARE', 15000.0, 15000.0, 250.0, 'APPROVED')
ON DUPLICATE KEY UPDATE updated_at = NOW();

INSERT INTO rating_rules (rule_id, rule_name, species_code, breed_code, min_age_years, max_age_years, factor, active) VALUES
(1, 'DOG_BASE_FACTOR', 'DOG', NULL, 0, 15, 1.20, TRUE),
(2, 'CAT_BASE_FACTOR', 'CAT', NULL, 0, 20, 1.00, TRUE),
(3, 'GOLDEN_RETRIEVER_FACTOR', 'DOG', 'GOLDEN_RETRIEVER', 0, 15, 1.15, TRUE),
(4, 'SENIOR_PET_SURCHARGE', 'DOG', NULL, 8, 25, 1.35, TRUE)
ON DUPLICATE KEY UPDATE updated_at = NOW();

INSERT INTO premium_calculations (calculation_id, quote_id, base_rate, breed_multiplier, age_multiplier, health_multiplier, calculated_annual_premium, calculated_monthly_premium, currency, rule_set_version) VALUES
(1, 1, 600.0, 1.15, 1.10, 1.05, 850.0, 70.83, 'USD', 'v1.0'),
(2, 2, 400.0, 1.05, 1.05, 1.00, 520.0, 43.33, 'USD', 'v1.0')
ON DUPLICATE KEY UPDATE calculated_at = NOW();

-- ================================================================================
-- 5. POLICY SERVICE (Database: policy_db)
-- ================================================================================
CREATE DATABASE IF NOT EXISTS policy_db;
USE policy_db;

SET FOREIGN_KEY_CHECKS = 0;
DROP TABLE IF EXISTS policy_status_history, coverages, policies;
SET FOREIGN_KEY_CHECKS = 1;

-- 12. policies
CREATE TABLE IF NOT EXISTS policies (
    policy_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    id BIGINT,
    policy_number VARCHAR(100) NOT NULL UNIQUE,
    customer_id BIGINT NOT NULL,
    pet_id BIGINT NOT NULL,
    quote_id BIGINT NOT NULL,
    status VARCHAR(50) NOT NULL DEFAULT 'ACTIVE',
    coverage_amount DOUBLE NOT NULL,
    annual_premium DOUBLE NOT NULL,
    monthly_premium DOUBLE NOT NULL,
    deductible_amount DOUBLE NOT NULL,
    deductible DOUBLE,
    currency VARCHAR(10) DEFAULT 'USD',
    effective_date DATE,
    start_date DATE,
    expiry_date DATE,
    end_date DATE,
    cancelled_at TIMESTAMP NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 13. coverages
CREATE TABLE IF NOT EXISTS coverages (
    coverage_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    id BIGINT,
    policy_id BIGINT NOT NULL,
    coverage_type VARCHAR(100) NOT NULL,
    limit_amount DOUBLE NOT NULL,
    deductible_amount DOUBLE NOT NULL,
    status VARCHAR(50) NOT NULL DEFAULT 'ACTIVE',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT fk_cov_policy FOREIGN KEY (policy_id) REFERENCES policies(policy_id) ON DELETE CASCADE
);

-- 14. policy_status_history
CREATE TABLE IF NOT EXISTS policy_status_history (
    history_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    id BIGINT,
    policy_id BIGINT NOT NULL,
    old_status VARCHAR(50),
    new_status VARCHAR(50) NOT NULL,
    reason VARCHAR(255),
    changed_by VARCHAR(100),
    changed_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT fk_pol_history FOREIGN KEY (policy_id) REFERENCES policies(policy_id) ON DELETE CASCADE
);

-- Seed Data for policy_db
INSERT INTO policies (policy_id, policy_number, customer_id, pet_id, quote_id, status, coverage_amount, annual_premium, monthly_premium, deductible_amount, currency, effective_date, expiry_date) VALUES
(1, 'POL-2024-99881', 1, 1, 1, 'ACTIVE', 25000.0, 850.0, 70.83, 500.0, 'USD', '2024-01-01', '2025-01-01'),
(2, 'POL-2024-99882', 1, 2, 2, 'ACTIVE', 15000.0, 520.0, 43.33, 250.0, 'USD', '2024-02-01', '2025-02-01')
ON DUPLICATE KEY UPDATE updated_at = NOW();

INSERT INTO coverages (coverage_id, policy_id, coverage_type, limit_amount, deductible_amount, status) VALUES
(1, 1, 'LIFELONG_CONTINUITY', 20000.0, 500.0, 'ACTIVE'),
(2, 1, 'EMERGENCY_VET', 5000.0, 250.0, 'ACTIVE'),
(3, 2, 'LIFELONG_CONTINUITY', 15000.0, 250.0, 'ACTIVE')
ON DUPLICATE KEY UPDATE status = 'ACTIVE';

INSERT INTO policy_status_history (history_id, policy_id, old_status, new_status, reason, changed_by) VALUES
(1, 1, 'PENDING_PAYMENT', 'ACTIVE', 'Initial premium received and verified', 'SYSTEM'),
(2, 2, 'PENDING_PAYMENT', 'ACTIVE', 'Initial premium received and verified', 'SYSTEM')
ON DUPLICATE KEY UPDATE changed_at = NOW();

-- ================================================================================
-- 6. CLAIMS SERVICE (Database: claims_db)
-- ================================================================================
CREATE DATABASE IF NOT EXISTS claims_db;
USE claims_db;

SET FOREIGN_KEY_CHECKS = 0;
DROP TABLE IF EXISTS claim_information_requests, claim_status_history, fraud_assessments, investigations, event_verifications, claim_documents, claims;
SET FOREIGN_KEY_CHECKS = 1;

-- 15. claims
CREATE TABLE IF NOT EXISTS claims (
    claim_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    id BIGINT,
    claim_number VARCHAR(100) NOT NULL UNIQUE,
    policy_id BIGINT NOT NULL,
    pet_id BIGINT NOT NULL,
    customer_id BIGINT NOT NULL,
    claim_type VARCHAR(50) NOT NULL,
    trigger_event VARCHAR(100) NOT NULL,
    status VARCHAR(50) NOT NULL DEFAULT 'SUBMITTED',
    approved_amount DOUBLE,
    payout_frequency VARCHAR(50),
    payout_start_date DATE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 16. claim_documents
CREATE TABLE IF NOT EXISTS claim_documents (
    document_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    id BIGINT,
    claim_id BIGINT NOT NULL,
    document_type VARCHAR(50) NOT NULL,
    file_url VARCHAR(500) NOT NULL,
    file_name VARCHAR(255) NOT NULL,
    file_size_bytes BIGINT NOT NULL,
    mime_type VARCHAR(100) NOT NULL,
    verification_status VARCHAR(50) NOT NULL DEFAULT 'PENDING',
    uploaded_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    verified_at TIMESTAMP NULL,
    rejection_reason VARCHAR(255),
    CONSTRAINT fk_doc_claim FOREIGN KEY (claim_id) REFERENCES claims(claim_id) ON DELETE CASCADE
);

-- 17. event_verifications
CREATE TABLE IF NOT EXISTS event_verifications (
    event_verification_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    id BIGINT,
    claim_id BIGINT NOT NULL,
    verification_type VARCHAR(50) NOT NULL,
    verification_source VARCHAR(100) NOT NULL,
    status VARCHAR(50) NOT NULL DEFAULT 'PENDING',
    verified_by VARCHAR(100),
    verified_at TIMESTAMP NULL,
    notes TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    CONSTRAINT fk_ev_claim FOREIGN KEY (claim_id) REFERENCES claims(claim_id) ON DELETE CASCADE
);

-- 18. investigations
CREATE TABLE IF NOT EXISTS investigations (
    investigation_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    id BIGINT,
    claim_id BIGINT NOT NULL,
    investigator_name VARCHAR(150) NOT NULL,
    reason VARCHAR(255) NOT NULL,
    findings TEXT,
    status VARCHAR(50) NOT NULL DEFAULT 'OPEN',
    outcome VARCHAR(50),
    initiated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    completed_at TIMESTAMP NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    CONSTRAINT fk_inv_claim FOREIGN KEY (claim_id) REFERENCES claims(claim_id) ON DELETE CASCADE
);

-- 19. fraud_assessments
CREATE TABLE IF NOT EXISTS fraud_assessments (
    fraud_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    id BIGINT,
    claim_id BIGINT NOT NULL,
    risk_score DOUBLE NOT NULL,
    fraud_tier VARCHAR(50) NOT NULL,
    flag_reasons TEXT,
    model_version VARCHAR(50) NOT NULL DEFAULT 'v1.0',
    assessed_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    requires_investigation BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    CONSTRAINT fk_fraud_claim FOREIGN KEY (claim_id) REFERENCES claims(claim_id) ON DELETE CASCADE
);

-- 20. claim_status_history
CREATE TABLE IF NOT EXISTS claim_status_history (
    history_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    claim_id BIGINT NOT NULL,
    old_status VARCHAR(50),
    new_status VARCHAR(50) NOT NULL,
    changed_by VARCHAR(100),
    reason VARCHAR(255),
    changed_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT fk_csh_claim FOREIGN KEY (claim_id) REFERENCES claims(claim_id) ON DELETE CASCADE
);

-- 21. claim_information_requests
CREATE TABLE IF NOT EXISTS claim_information_requests (
    request_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    claim_id BIGINT NOT NULL,
    requested_by VARCHAR(100),
    request_reason VARCHAR(255),
    document_type_requested VARCHAR(100),
    status VARCHAR(50) DEFAULT 'PENDING',
    due_date DATE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    responded_at TIMESTAMP NULL,
    CONSTRAINT fk_cir_claim FOREIGN KEY (claim_id) REFERENCES claims(claim_id) ON DELETE CASCADE
);

-- Seed Data for claims_db
INSERT INTO claims (claim_id, claim_number, policy_id, pet_id, customer_id, claim_type, trigger_event, status, approved_amount, payout_frequency, payout_start_date) VALUES
(1, 'CLM-2024-00101', 1, 1, 1, 'CONTINUITY_TRIGGER', 'OWNER_HOSPITALIZATION', 'APPROVED', 25000.0, 'MONTHLY', '2024-06-01')
ON DUPLICATE KEY UPDATE updated_at = NOW();

INSERT INTO claim_documents (document_id, claim_id, document_type, file_url, file_name, file_size_bytes, mime_type, verification_status, verified_at) VALUES
(1, 1, 'HOSPITAL_ADMISSION_RECORD', 'https://petcontinuity-records.s3.amazonaws.com/claims/1/hospital_admission_letter.pdf', 'hospital_admission_letter.pdf', 342100, 'application/pdf', 'VERIFIED', NOW())
ON DUPLICATE KEY UPDATE verified_at = NOW();

INSERT INTO event_verifications (event_verification_id, claim_id, verification_type, verification_source, status, verified_by, verified_at, notes) VALUES
(1, 1, 'HOSPITALIZATION', 'Springfield General Hospital Admin Desk', 'CONFIRMED', 'mike_claims', NOW(), 'Admission date and projected stay verified directly with attending physician office')
ON DUPLICATE KEY UPDATE updated_at = NOW();

INSERT INTO fraud_assessments (fraud_id, claim_id, risk_score, fraud_tier, flag_reasons, model_version, requires_investigation) VALUES
(1, 1, 8.5, 'LOW', 'None. Legitimate medical documentation provided', 'v1.0', FALSE)
ON DUPLICATE KEY UPDATE updated_at = NOW();

INSERT INTO claim_status_history (history_id, claim_id, old_status, new_status, changed_by, reason) VALUES
(1, 1, 'SUBMITTED', 'UNDER_REVIEW', 'mike_claims', 'Document verification initiated'),
(2, 1, 'UNDER_REVIEW', 'APPROVED', 'mike_claims', 'Hospitalization confirmed and verified')
ON DUPLICATE KEY UPDATE changed_at = NOW();

-- ================================================================================
-- 7. PAYMENT & FUND SERVICE (Database: payment_fund_db)
-- ================================================================================
CREATE DATABASE IF NOT EXISTS payment_fund_db;
USE payment_fund_db;

SET FOREIGN_KEY_CHECKS = 0;
DROP TABLE IF EXISTS fund_status_history, expenses, disbursements, fund_transactions, pet_care_funds, premium_payments;
SET FOREIGN_KEY_CHECKS = 1;

-- 22. premium_payments
CREATE TABLE IF NOT EXISTS premium_payments (
    payment_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    id BIGINT,
    policy_id BIGINT NOT NULL,
    customer_id BIGINT,
    currency VARCHAR(10) DEFAULT 'USD',
    idempotency_key VARCHAR(100),
    amount DOUBLE NOT NULL,
    payment_reference VARCHAR(100),
    payment_method VARCHAR(50),
    status VARCHAR(50) NOT NULL,
    failure_reason VARCHAR(255),
    initiated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    payment_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    completed_at TIMESTAMP NULL,
    failed_at TIMESTAMP NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 23. pet_care_funds
CREATE TABLE IF NOT EXISTS pet_care_funds (
    fund_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    id BIGINT,
    policy_id BIGINT NOT NULL UNIQUE,
    pet_id BIGINT NOT NULL,
    approved_claim_id BIGINT,
    currency VARCHAR(10) DEFAULT 'USD',
    total_amount DOUBLE NOT NULL,
    available_amount DOUBLE NOT NULL,
    monthly_allowance DOUBLE NOT NULL,
    veterinary_reserve DOUBLE NOT NULL,
    emergency_reserve DOUBLE NOT NULL,
    status VARCHAR(50) NOT NULL DEFAULT 'ACTIVE',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    version INT DEFAULT 1
);

-- 24. fund_transactions
CREATE TABLE IF NOT EXISTS fund_transactions (
    transaction_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    id BIGINT,
    fund_id BIGINT NOT NULL,
    transaction_type VARCHAR(50) NOT NULL,
    currency VARCHAR(10) DEFAULT 'USD',
    amount DOUBLE NOT NULL,
    balance_after DOUBLE NOT NULL,
    external_reference VARCHAR(100),
    idempotency_key VARCHAR(100),
    reference_id VARCHAR(100),
    reversal_of_transaction_id BIGINT,
    description VARCHAR(255),
    status VARCHAR(50) NOT NULL DEFAULT 'SUCCESS',
    created_by VARCHAR(100),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT fk_txn_fund FOREIGN KEY (fund_id) REFERENCES pet_care_funds(fund_id) ON DELETE CASCADE
);

-- 25. disbursements
CREATE TABLE IF NOT EXISTS disbursements (
    disbursement_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    id BIGINT,
    fund_id BIGINT NOT NULL,
    currency VARCHAR(10) DEFAULT 'USD',
    amount DOUBLE NOT NULL,
    assignment_id BIGINT,
    caretaker_id BIGINT,
    eligibility_check_id BIGINT,
    idempotency_key VARCHAR(100),
    disbursement_type VARCHAR(50) DEFAULT 'MONTHLY_ALLOWANCE',
    eligibility_status VARCHAR(50) DEFAULT 'ELIGIBLE',
    benefit_month VARCHAR(20),
    status VARCHAR(50) NOT NULL DEFAULT 'PROCESSED',
    scheduled_date DATE,
    processed_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT fk_disb_fund FOREIGN KEY (fund_id) REFERENCES pet_care_funds(fund_id) ON DELETE CASCADE
);

-- 26. expenses
CREATE TABLE IF NOT EXISTS expenses (
    expense_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    id BIGINT,
    fund_id BIGINT NOT NULL,
    assignment_id BIGINT,
    caretaker_id BIGINT,
    expense_type VARCHAR(50) NOT NULL,
    currency VARCHAR(10) DEFAULT 'USD',
    amount DOUBLE NOT NULL,
    vendor_name VARCHAR(150),
    document_reference VARCHAR(255),
    approval_status VARCHAR(50) DEFAULT 'APPROVED',
    idempotency_key VARCHAR(100),
    decided_by VARCHAR(100),
    decided_at TIMESTAMP NULL,
    decision_reason VARCHAR(255),
    submitted_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    approved_at TIMESTAMP NULL,
    expense_date DATE,
    CONSTRAINT fk_exp_fund FOREIGN KEY (fund_id) REFERENCES pet_care_funds(fund_id) ON DELETE CASCADE
);

-- 27. fund_status_history
CREATE TABLE IF NOT EXISTS fund_status_history (
    history_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    fund_id BIGINT NOT NULL,
    old_status VARCHAR(50),
    new_status VARCHAR(50) NOT NULL,
    changed_by VARCHAR(100),
    reason VARCHAR(255),
    changed_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT fk_fsh_fund FOREIGN KEY (fund_id) REFERENCES pet_care_funds(fund_id) ON DELETE CASCADE
);

-- Seed Data for payment_fund_db
INSERT INTO premium_payments (payment_id, policy_id, customer_id, currency, amount, payment_reference, payment_method, status, initiated_at, completed_at) VALUES
(1, 1, 1, 'USD', 70.83, 'PAY-REC-2024-01-001', 'CREDIT_CARD', 'SUCCESS', NOW(), NOW()),
(2, 2, 1, 'USD', 43.33, 'PAY-REC-2024-02-002', 'ACH_TRANSFER', 'SUCCESS', NOW(), NOW())
ON DUPLICATE KEY UPDATE status = 'SUCCESS';

INSERT INTO pet_care_funds (fund_id, policy_id, pet_id, approved_claim_id, currency, total_amount, available_amount, monthly_allowance, veterinary_reserve, emergency_reserve, status) VALUES
(1, 1, 1, 1, 'USD', 25000.0, 24500.0, 500.0, 4000.0, 2000.0, 'ACTIVE')
ON DUPLICATE KEY UPDATE updated_at = NOW();

INSERT INTO fund_transactions (transaction_id, fund_id, transaction_type, currency, amount, balance_after, external_reference, description, status) VALUES
(1, 1, 'INITIAL_DEPOSIT', 'USD', 25000.0, 25000.0, 'CLAIM-PAYOUT-00101', 'Initial approved claim funding', 'SUCCESS'),
(2, 1, 'DISBURSEMENT', 'USD', 500.0, 24500.0, 'DISB-2024-06-001', 'June monthly care allowance', 'SUCCESS')
ON DUPLICATE KEY UPDATE status = 'SUCCESS';

INSERT INTO disbursements (disbursement_id, fund_id, currency, amount, assignment_id, caretaker_id, disbursement_type, eligibility_status, benefit_month, status, scheduled_date, processed_at) VALUES
(1, 1, 'USD', 500.0, 1, 1, 'MONTHLY_ALLOWANCE', 'ELIGIBLE', '2024-06', 'PROCESSED', '2024-06-01', NOW())
ON DUPLICATE KEY UPDATE status = 'PROCESSED';

INSERT INTO expenses (expense_id, fund_id, assignment_id, caretaker_id, expense_type, currency, amount, vendor_name, document_reference, approval_status, expense_date) VALUES
(1, 1, 1, 1, 'FOOD_AND_SUPPLIES', 'USD', 120.50, 'PetSmart Store #412', 'DOC-REC-00918', 'APPROVED', '2024-06-05')
ON DUPLICATE KEY UPDATE approval_status = 'APPROVED';

INSERT INTO fund_status_history (history_id, fund_id, old_status, new_status, changed_by, reason) VALUES
(1, 1, 'INITIALIZING', 'ACTIVE', 'SYSTEM', 'Fund seeded and claim payout finalized')
ON DUPLICATE KEY UPDATE changed_at = NOW();

-- ================================================================================
-- 8. CARE & VERIFICATION SERVICE (Database: care_verification_db)
-- ================================================================================
CREATE DATABASE IF NOT EXISTS care_verification_db;
USE care_verification_db;

SET FOREIGN_KEY_CHECKS = 0;
DROP TABLE IF EXISTS care_transfers, monthly_eligible_checks, verification_history, pet_verifications, care_plans, caretaker_verification, pet_caretaker_assignments, caretakers;
SET FOREIGN_KEY_CHECKS = 1;

-- 28. caretakers
CREATE TABLE IF NOT EXISTS caretakers (
    caretaker_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    id BIGINT,
    user_id BIGINT,
    customer_id BIGINT,
    pet_id BIGINT,
    name VARCHAR(150),
    full_name VARCHAR(150),
    phone VARCHAR(50),
    email VARCHAR(150),
    identity_status VARCHAR(50) DEFAULT 'VERIFIED',
    relationship VARCHAR(50) DEFAULT 'PRIMARY',
    caretaker_type VARCHAR(50) DEFAULT 'PRIMARY',
    status VARCHAR(50) NOT NULL DEFAULT 'ACTIVE',
    verification_status VARCHAR(50) DEFAULT 'VERIFIED',
    availability_status VARCHAR(50) DEFAULT 'ACTIVE',
    priority INT DEFAULT 1,
    address VARCHAR(255),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    version INT DEFAULT 1
);

-- 29. pet_caretaker_assignments
CREATE TABLE IF NOT EXISTS pet_caretaker_assignments (
    assignment_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    pet_id BIGINT NOT NULL,
    caretaker_id BIGINT NOT NULL,
    priority INT DEFAULT 1,
    caretaker_type VARCHAR(50) DEFAULT 'PRIMARY',
    relationship VARCHAR(50),
    availability_status VARCHAR(50) DEFAULT 'AVAILABLE',
    assignment_status VARCHAR(50) DEFAULT 'ACTIVE',
    nominated_by BIGINT,
    effective_from DATE,
    effective_to DATE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    version INT DEFAULT 1,
    CONSTRAINT fk_pca_caretaker FOREIGN KEY (caretaker_id) REFERENCES caretakers(caretaker_id) ON DELETE CASCADE
);

-- 30. caretaker_verification
CREATE TABLE IF NOT EXISTS caretaker_verification (
    verification_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    caretaker_id BIGINT NOT NULL,
    verification_type VARCHAR(50) DEFAULT 'IDENTITY',
    verification_method VARCHAR(50) DEFAULT 'DOCUMENT',
    verification_status VARCHAR(50) DEFAULT 'VERIFIED',
    evidence_reference VARCHAR(255),
    verification_by VARCHAR(100),
    verified_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    expires_at TIMESTAMP NULL,
    failure_reason VARCHAR(255),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT fk_cv_caretaker FOREIGN KEY (caretaker_id) REFERENCES caretakers(caretaker_id) ON DELETE CASCADE
);

-- 31. care_plans
CREATE TABLE IF NOT EXISTS care_plans (
    care_plan_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    id BIGINT,
    pet_id BIGINT NOT NULL,
    primary_caretaker_id BIGINT,
    backup_caretaker_id BIGINT,
    feeding_instructions TEXT,
    medication_instructions TEXT,
    vet_details TEXT,
    vet_contact TEXT,
    routine_details TEXT,
    special_requirements TEXT,
    special_needs TEXT,
    status VARCHAR(50) DEFAULT 'ACTIVE',
    plan_version INT DEFAULT 1,
    effective_from DATE,
    effective_to DATE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 32. pet_verifications
CREATE TABLE IF NOT EXISTS pet_verifications (
    pet_verification_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    id BIGINT,
    pet_id BIGINT NOT NULL,
    caretaker_id BIGINT NOT NULL,
    verification_method VARCHAR(50) DEFAULT 'MOBILE_CHECKIN',
    verification_status VARCHAR(50) DEFAULT 'PASSED',
    status VARCHAR(50) DEFAULT 'PASSED',
    evidence_reference VARCHAR(255),
    verification_date VARCHAR(50),
    verified_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    next_verification_date DATE,
    remarks TEXT,
    notes TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT fk_pv_caretaker FOREIGN KEY (caretaker_id) REFERENCES caretakers(caretaker_id) ON DELETE CASCADE
);

-- 33. verification_history
CREATE TABLE IF NOT EXISTS verification_history (
    history_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    pet_id BIGINT NOT NULL,
    caretaker_id BIGINT NOT NULL,
    verification_type VARCHAR(50),
    old_status VARCHAR(50),
    new_status VARCHAR(50),
    verification_method VARCHAR(50),
    verified_by VARCHAR(100),
    verified_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    remarks TEXT,
    CONSTRAINT fk_vh_caretaker FOREIGN KEY (caretaker_id) REFERENCES caretakers(caretaker_id) ON DELETE CASCADE
);

-- 34. monthly_eligible_checks
CREATE TABLE IF NOT EXISTS monthly_eligible_checks (
    eligibility_check_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    pet_id BIGINT NOT NULL,
    assignment_id BIGINT NOT NULL,
    fund_id BIGINT NOT NULL,
    eligibility_month VARCHAR(20) NOT NULL,
    caretaker_verified BOOLEAN DEFAULT TRUE,
    pet_verified BOOLEAN DEFAULT TRUE,
    policy_active BOOLEAN DEFAULT TRUE,
    fund_active BOOLEAN DEFAULT TRUE,
    eligible BOOLEAN DEFAULT TRUE,
    failure_reason VARCHAR(255),
    evaluated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    correlation_id VARCHAR(100),
    CONSTRAINT fk_mec_assignment FOREIGN KEY (assignment_id) REFERENCES pet_caretaker_assignments(assignment_id) ON DELETE CASCADE
);

-- 35. care_transfers
CREATE TABLE IF NOT EXISTS care_transfers (
    transfer_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    pet_id BIGINT NOT NULL,
    from_assignment_id BIGINT,
    to_assignment_id BIGINT,
    reason VARCHAR(255),
    effective_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    initiated_by VARCHAR(100),
    status VARCHAR(50) DEFAULT 'COMPLETED',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT fk_ct_from FOREIGN KEY (from_assignment_id) REFERENCES pet_caretaker_assignments(assignment_id) ON DELETE SET NULL,
    CONSTRAINT fk_ct_to FOREIGN KEY (to_assignment_id) REFERENCES pet_caretaker_assignments(assignment_id) ON DELETE SET NULL
);

-- Seed Data for care_verification_db
INSERT INTO caretakers (caretaker_id, user_id, customer_id, pet_id, name, phone, email, identity_status, relationship, caretaker_type, status, verification_status, availability_status, priority, address) VALUES
(1, 4, 1, 1, 'Emma Watson', '+1-555-0244', 'emma.caretaker@example.com', 'VERIFIED', 'PRIMARY', 'PRIMARY', 'ACTIVE', 'VERIFIED', 'ACTIVE', 1, '124 Pet Haven Lane, Springfield, OR 97477'),
(2, NULL, 1, 1, 'David Miller', '+1-555-0388', 'david.miller@example.com', 'VERIFIED', 'BACKUP', 'SECONDARY', 'ACTIVE', 'VERIFIED', 'AVAILABLE', 2, '55 Elm St, Springfield, OR 97477')
ON DUPLICATE KEY UPDATE updated_at = NOW();

INSERT INTO pet_caretaker_assignments (assignment_id, pet_id, caretaker_id, priority, caretaker_type, relationship, availability_status, assignment_status, nominated_by, effective_from) VALUES
(1, 1, 1, 1, 'PRIMARY', 'SISTER', 'AVAILABLE', 'ACTIVE', 1, '2024-01-01'),
(2, 1, 2, 2, 'BACKUP', 'FRIEND', 'AVAILABLE', 'ACTIVE', 1, '2024-01-01')
ON DUPLICATE KEY UPDATE updated_at = NOW();

INSERT INTO caretaker_verification (verification_id, caretaker_id, verification_type, verification_method, verification_status, evidence_reference, verification_by, verified_at) VALUES
(1, 1, 'IDENTITY', 'GOVERNMENT_ID', 'VERIFIED', 'DOC-VERIF-ID-9921', 'emma_caretaker', NOW()),
(2, 2, 'IDENTITY', 'GOVERNMENT_ID', 'VERIFIED', 'DOC-VERIF-ID-9922', 'david_miller', NOW())
ON DUPLICATE KEY UPDATE verification_status = 'VERIFIED';

INSERT INTO care_plans (care_plan_id, pet_id, primary_caretaker_id, backup_caretaker_id, feeding_instructions, medication_instructions, vet_details, routine_details, special_requirements, status, plan_version, effective_from) VALUES
(1, 1, 1, 2, '2 cups premium kibble twice daily (8am & 6pm). Fresh water bowl refilled daily.', 'Glucosamine joint supplement chew once every morning with breakfast.', 'Springfield Animal Clinic - Dr. Robert Hayes (555-0199)', 'Daily 45-minute walk in morning, backyard play in evening.', 'Allergic to chicken; feed grain-free salmon formula only.', 'ACTIVE', 1, '2024-01-01')
ON DUPLICATE KEY UPDATE updated_at = NOW();

INSERT INTO pet_verifications (pet_verification_id, pet_id, caretaker_id, verification_method, verification_status, evidence_reference, verified_at, next_verification_date, remarks) VALUES
(1, 1, 1, 'VIDEO_INSPECTION', 'PASSED', 'EVID-VID-202406-001', NOW(), DATE_ADD(CURRENT_DATE(), INTERVAL 30 DAY), 'Buddy verified happy, healthy, well-fed, and interactive.')
ON DUPLICATE KEY UPDATE verified_at = NOW();

INSERT INTO verification_history (history_id, pet_id, caretaker_id, verification_type, old_status, new_status, verification_method, verified_by, verified_at, remarks) VALUES
(1, 1, 1, 'MONTHLY_CHECKIN', 'PENDING', 'PASSED', 'VIDEO_INSPECTION', 'emma_caretaker', NOW(), 'Initial post-trigger monthly welfare confirmation passed.')
ON DUPLICATE KEY UPDATE verified_at = NOW();

INSERT INTO monthly_eligible_checks (eligibility_check_id, pet_id, assignment_id, fund_id, eligibility_month, caretaker_verified, pet_verified, policy_active, fund_active, eligible, evaluated_at, correlation_id) VALUES
(1, 1, 1, 1, '2024-06', TRUE, TRUE, TRUE, TRUE, TRUE, NOW(), 'CORR-ELIG-202406-001')
ON DUPLICATE KEY UPDATE evaluated_at = NOW();

INSERT INTO care_transfers (transfer_id, pet_id, from_assignment_id, to_assignment_id, reason, effective_at, initiated_by, status) VALUES
(1, 1, NULL, 1, 'Initial policy trigger handover to primary caretaker', NOW(), 'mike_claims', 'COMPLETED')
ON DUPLICATE KEY UPDATE status = 'COMPLETED';
